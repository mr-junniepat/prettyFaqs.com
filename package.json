{
  "name": "prettyfaqs.com",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "add:component": "npm run --workspace @repo/ui ui:add"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/nodemailer": "^6.4.14",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "dotenv-cli": "^7.4.1",
    "entities": "^4.5.0",
    "nodemailer": "^6.9.12",
    "react-router-dom": "^6.22.3",
    "zod": "^3.22.4"
  }
}
